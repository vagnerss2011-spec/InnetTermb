# .NET
bin/
obj/
*.user
.vs/
artifacts/
TestResults/

# Node (terminal WebView2/xterm)
node_modules/
dist/

# Build nativo (NDesk)
build/

# Local / sessão
.claude/settings.local.json
.DS_Store
Thumbs.db

# Deploy: o .env do operador tem a senha do Postgres e a chave do JWT.
# O modelo versionado é o .env.example (sem valores).
.env
.env.local
