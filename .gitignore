# .NET
bin/
obj/
*.user
.vs/
artifacts/
TestResults/

# Node (terminal WebView2/xterm)
node_modules/
dist/

# SEC-008: compiled wwwroot bundles are build artefacts — generate via `npm ci && node build.js`
# Committed once as a bootstrap; CI must regenerate and verify integrity.
# Uncomment the lines below when CI generates these artefacts reliably:
# src/RemoteOps.Desktop/Terminal/wwwroot/js/terminal.bundle.js
# src/RemoteOps.Desktop/Terminal/wwwroot/css/xterm.css

# Build nativo (NDesk)
build/

# Local / sessão
.claude/settings.local.json
.DS_Store
Thumbs.db
