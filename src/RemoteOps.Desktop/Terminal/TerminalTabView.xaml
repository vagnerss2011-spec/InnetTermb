<UserControl x:Class="RemoteOps.Desktop.Terminal.TerminalTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid Background="{DynamicResource Brush.Bg.Canvas}">
        <!-- Loading placeholder shown until WebView2 is ready -->
        <TextBlock x:Name="_loadingText"
                   Text="Conectando..."
                   Foreground="{DynamicResource Brush.Text.Tertiary}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="13"/>
        <wv2:WebView2 x:Name="_webView" Visibility="Collapsed"/>
    </Grid>
</UserControl>
