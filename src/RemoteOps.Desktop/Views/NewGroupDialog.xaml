<Window x:Class="RemoteOps.Desktop.Views.NewGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Novo grupo" Height="160" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Brush.Bg.App}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="{DynamicResource Font.Family.Base}"
        FontSize="{DynamicResource Font.Size.Body}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" IsCancel="True" MinWidth="88" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Salvar" Command="{Binding SaveCommand}" IsDefault="True"
                    Style="{DynamicResource Button.Primary}" MinWidth="88" Padding="10,4"/>
        </StackPanel>

        <StackPanel VerticalAlignment="Top">
            <TextBlock Text="Nome do grupo" Style="{DynamicResource Text.BodyStrong}" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </DockPanel>
</Window>
