<Window x:Class="RemoteOps.Desktop.Views.CredentialDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="Auto" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Brush.Bg.App}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="{DynamicResource Font.Family.Base}"
        FontSize="{DynamicResource Font.Size.Body}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <StackPanel Margin="16">
        <TextBlock Text="{Binding Title}" Style="{DynamicResource Text.TitleLg}" Margin="0,0,0,12"/>

        <StackPanel Visibility="{Binding ShowNameUsername, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Name" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBlock Text="Username" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Visibility="{Binding ShowTypePicker, Converter={StaticResource BoolToVis}}" Margin="0,0,0,10">
            <TextBlock Text="Type" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <ComboBox x:Name="TypePicker" SelectedIndex="0" SelectionChanged="TypePicker_SelectionChanged">
                <ComboBoxItem Content="Password"/>
                <ComboBoxItem Content="SSH key"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Visibility="{Binding ShowPassword, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Password" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <PasswordBox x:Name="PasswordField" Height="28"
                         Background="{DynamicResource Brush.Bg.Surface}"
                         Foreground="{DynamicResource Brush.Text.Primary}"
                         BorderBrush="{DynamicResource Brush.Border.Default}"
                         Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Visibility="{Binding ShowPrivateKey, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Private key (OpenSSH/PEM)" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <TextBox x:Name="PrivateKeyField" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas"
                     MinHeight="90" MaxHeight="150" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" Margin="0,0,0,4"/>
            <Button Content="Browse…" Click="BrowseKey_Click" HorizontalAlignment="Left" Padding="10,3" Margin="0,0,0,10"/>
            <TextBlock Text="Passphrase (optional)" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,0,2"/>
            <PasswordBox x:Name="PassphraseField" Height="28"
                         Background="{DynamicResource Brush.Bg.Surface}"
                         Foreground="{DynamicResource Brush.Text.Primary}"
                         BorderBrush="{DynamicResource Brush.Border.Default}"
                         Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="Cancel" IsCancel="True" MinWidth="80" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" MinWidth="80" Padding="10,4"/>
        </StackPanel>
    </StackPanel>
</Window>
