<UserControl x:Class="RemoteOps.Desktop.NDesk.NDeskTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource Brush.Bg.Canvas}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Painel do operador -->
        <Grid Grid.Column="0" DataContext="{Binding Operator}" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Operador" Foreground="{DynamicResource Brush.Text.Primary}" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox Width="140" Text="{Binding TicketIdInput, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Ticket (gere um novo ou cole um existente)"/>
                <Button Content="Gerar ticket" Command="{Binding GenerateTicketCommand}" Margin="6,0,0,0"/>
                <Button Content="Conectar" Command="{Binding ConnectCommand}" Margin="6,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Estado: " Foreground="{DynamicResource Brush.Text.Tertiary}" FontSize="11"/>
                <TextBlock Text="{Binding State}" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Status.Error}" FontSize="11" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Superfície de vídeo placeholder -->
            <Border Grid.Row="3" Background="{DynamicResource Brush.Bg.Canvas}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" Margin="0,0,0,8">
                <TextBlock Text="Tela remota (placeholder)" Foreground="{DynamicResource Brush.Text.Tertiary}" FontStyle="Italic"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Opacity" Value="0.3"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State}" Value="Connected">
                                <Setter Property="Opacity" Value="1"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!-- Banner + Encerrar sempre acessível durante a sessão -->
            <Border Grid.Row="4" Background="{DynamicResource Brush.Status.CriticalSurface}" Padding="8,6">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSessionActive}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Sessão NDesk em andamento — visível para o usuário atendido"
                               Foreground="{DynamicResource Brush.Text.Primary}" FontSize="11" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Encerrar" Command="{Binding EndCommand}"/>
                </Grid>
            </Border>
        </Grid>

        <Rectangle Grid.Column="1" Fill="{DynamicResource Brush.Border.Default}"/>

        <!-- Painel mock do lado atendido -->
        <Grid Grid.Column="2" DataContext="{Binding Assisted}" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Lado atendido (mock)" Foreground="{DynamicResource Brush.Text.Primary}" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Nenhuma solicitação pendente." Foreground="{DynamicResource Brush.Text.Tertiary}" FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPendingRequest}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPendingRequest}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,4">
                        <Run Text="Operador: "/>
                        <Run Text="{Binding PendingConsent.OperatorDisplayName}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,4">
                        <Run Text="Empresa: "/>
                        <Run Text="{Binding PendingConsent.CompanyName}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,4">
                        <Run Text="Ticket: "/>
                        <Run Text="{Binding PendingConsent.TicketId}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,4">
                        <Run Text="Modo solicitado: "/>
                        <Run Text="{Binding PendingConsent.RequestedMode}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,8">
                        <Run Text="Permissões: "/>
                        <Run Text="{Binding PermissionsRequestedText}" FontWeight="SemiBold"/>
                    </TextBlock>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Aceitar" Command="{Binding AcceptCommand}" Margin="0,0,6,0"/>
                        <Button Content="Recusar" Command="{Binding DeclineCommand}"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="{Binding State}" Foreground="{DynamicResource Brush.Text.Tertiary}" FontSize="11" Margin="0,8,0,8"/>
                <Button Content="Encerrar" Command="{Binding EndCommand}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
