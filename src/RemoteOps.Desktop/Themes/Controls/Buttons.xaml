<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Button — estilo implícito (TargetType sem x:Key): aplica-se a todo Button do app
        automaticamente ao mesclar este dicionário, sem precisar tocar Style="" em cada View.

        Variantes nomeadas (Button.Primary / Button.Danger) são BasedOn o estilo implícito e
        só trocam a cor de preenchimento — usadas nos botões cujo papel já é inequívoco pelo
        Command ligado (Aceitar/Conectar = confirmação; Encerrar/Remover/Recusar = destrutivo),
        reforçando visualmente o princípio de "revogação imediata visível" do CLAUDE.md sem
        mudar nenhum binding.
    -->

    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Brush.Bg.SurfaceRaised}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{DynamicResource Thickness.ControlPadding}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Family.Base}"/>
        <Setter Property="FontSize" Value="{DynamicResource Font.Size.Body}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Radius.Control}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Só o que é intrínseco ao Chrome fica aqui. Hover/pressed/foco foram pro
                             Style.Triggers (setando Background/BorderBrush do Button, que o Chrome lê
                             por TemplateBinding): setar Chrome.Background direto no template vencia o
                             Background das variantes Primary/Danger, deixando o botão primário escuro
                             com o texto claro por cima (ilegível) no hover. -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Bg.Hover}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Bg.Selected}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent.Base}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Button.Primary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource Brush.Accent.Base}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.OnAccent}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent.Base}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Accent.Hover}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent.Hover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Accent.Pressed}"/>
            </Trigger>
            <!-- Foco por teclado: a borda accent do estilo base era invisível (accent sobre accent).
                 Um anel escuro (OnAccent) sobre o preenchimento accent é visível na navegação por Tab. -->
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Text.OnAccent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Button.Danger" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource Brush.Bg.SurfaceRaised}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Error}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Status.Error}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Status.CriticalBase}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Botão "fantasma": sem borda/preenchimento — usado no X de fechar aba. -->
    <Style x:Key="Button.Ghost" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}"/>
        <Setter Property="Padding" Value="2"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.Bg.Hover}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
