<UserControl x:Class="RemoteOps.Desktop.Rdp.RdpTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wf="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration">
    <Grid Background="{DynamicResource Brush.Bg.Canvas}">
        <!-- Loading placeholder shown until the MSTSCAX control connects -->
        <TextBlock x:Name="_statusText"
                   Text="Conectando..."
                   Foreground="{DynamicResource Brush.Text.Tertiary}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="13"/>
        <wf:WindowsFormsHost x:Name="_formsHost" Visibility="Collapsed"/>
    </Grid>
</UserControl>
